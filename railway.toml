[build]
builder = "NIXPACKS"

[deploy]
startCommand = "next start -p ${PORT:-8080}"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
